// @flow

// import type { ComponentFile } from "../ComponentFile/types"
//
// export default function mapComponentFilesToRoles(
//   files: Map<string, ComponentFile>
// ): Array<Promise<any>> {
//   return Array.from(files.keys())
//
//   const roles: Array<string> = Array.from(files.keys())
// }
