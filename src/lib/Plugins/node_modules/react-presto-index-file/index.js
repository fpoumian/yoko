import path from "path"

module.exports = function(props, config) {
  return {
    name: "index",
    extension: "js",
    dir: props.path,
    role: "index",
    template: {
      name: "IndexFile.js",
      dir: path.resolve(__dirname, "templates")
    }
  }
}
